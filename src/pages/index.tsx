import * as C from '@/components'

import type {NextPage} from 'next'

const Home: NextPage = () => {
    return (
        <main>
            <C.SideMenu />
        </main>
    );
};

export default Home
